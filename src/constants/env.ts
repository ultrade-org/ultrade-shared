export const ALGO_NETWORK = process.env.ALGO_NETWORK;
export const NETWORK = ALGO_NETWORK && ALGO_NETWORK.toLowerCase();
export const WH_CORE_ID = Number(process.env.WH_CORE_ID);
export const SUPER_APP_ID = Number(process.env.SUPER_APP_ID);
export const CODEX_APP_ID = Number(process.env.CODEX_APP_ID);
export const MSG_PROC_ID = Number(process.env.MSG_PROCESSOR_ID);
