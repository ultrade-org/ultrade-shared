
export enum TradeStatus {
  Unconfirmed = "UNCONFIRMED",
  Confirmed = "CONFIRMED",
  Rejected = "REJECTED",
}

export enum TradeSubStatus {
  RESUBMITTED = "RESUBMITTED",
  PROCESSED = "PROCESSED",
  REMATCHED = "REMATCHED",
}
